import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// Render the App component inside the DOM element with the 'root' ID.
// ReactDOM.render is used to initialize the React application and insert the top-level component into the DOM.
ReactDOM.render(
  <App />, 
  document.getElementById('root')
);
